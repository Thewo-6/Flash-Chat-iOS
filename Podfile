# Uncomment the next line to define a global platform for your project
platform :ios, '17.6'

target 'Flash Chat IOS' do
  use_frameworks!

  # Pods for Flash Chat IOS
  pod 'CLTypingLabel'
  pod 'Firebase/Auth'
  pod 'Firebase/Firestore'

end
